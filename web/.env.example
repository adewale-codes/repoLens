# Server-side only (never NEXT_PUBLIC_): the RepoLens API base URL.
REPOLENS_API_URL=http://127.0.0.1:8000

# Public base URL of this site, used for share links and OG metadata.
# Falls back to the request host when unset (fine for local dev).
# SITE_URL=https://repolens.dev
